# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0          # async Postgres driver
alembic>=1.13.0

# Matching / evidence
rapidfuzz>=3.9.0         # fuzzy string evidence — use directly (THIRD_PARTY_NOTICES.md)
scipy>=1.13.0            # linear_sum_assignment — use directly

# Embeddings (P1 — installed now so P0→P1 transition is seamless)
sentence-transformers>=3.0.0   # BAAI/bge-small-en-v1.5

# LLM evidence
google-genai>=0.1.0     # gemini-3.7-flash via google.generativeai
instructor>=1.3.0              # structured LLM output (Instructor/Pydantic)

# Synthetic data generation
faker>=25.0.0                  # use directly (THIRD_PARTY_NOTICES.md)

# Evaluation
scikit-learn>=1.5.0            # calibration / evaluation utilities — use directly

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
jinja2
